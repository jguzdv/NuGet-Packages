name: JGUZDV.Linq

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    paths:
      - libraries/JGUZDV.Linq/**

jobs:
  call-build-and-test:
    uses: ./.github/workflows/pr_build.yml
    with:
      library-name: JGUZDV.Linq