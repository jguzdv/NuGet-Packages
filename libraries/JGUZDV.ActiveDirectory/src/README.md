# JGUZDV.ActiveDirectory

This module allows loading data for a specific user from Active Directory either as specific data types or as claims.

To use it just add the classes via DI
```csharp
services.AddPropertyReader();
services.AddClaimProvider();
```

