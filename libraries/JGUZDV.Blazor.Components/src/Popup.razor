@code {
    [Parameter, EditorRequired, NotNull]
    public RenderFragment? PopupAnchor { get; set; }

    [Parameter, EditorRequired, NotNull]
    public RenderFragment? PopupContent { get; set; }
}

