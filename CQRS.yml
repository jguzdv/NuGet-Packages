name: JGUZDV.CQRS

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    paths:
      - libraries/JGUZDV.CQRS/**

jobs:
  call-build-and-test:
    uses: ./.github/workflows/pr_build.yml
    with:
      library-name: JGUZDV.CQRS